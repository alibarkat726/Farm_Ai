name: orchard_advisory_app
description: Flutter client for Orchard Advisory Bot — diagnose apricot, apple, and cherry issues in Gilgit-Baltistan.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  http_parser: ^4.0.0
  provider: ^6.1.0
  image_picker: ^1.1.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
